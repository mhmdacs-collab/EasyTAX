EasyTAX is NOT an ERP.

Every feature must simplify the user experience.

Documents are the center of the system.

Offline First.

Saudi regulations first.

Never increase complexity.

Always prefer the smallest implementation that satisfies the business requirement.

Business logic must never depend on UI.

Settings configure behavior, not business workflows.

Do not introduce enterprise patterns unless explicitly requested.
